pipeline {
    agent any

    tools {
        jdk 'java17016'
        maven 'maven399'
    }

    environment {
        SERVICE_NAME = 'config-server'
        PROJECT_NAME = 'farmatodo'
    }

    options {
        timestamps()
        timeout(time: 20, unit: 'MINUTES')
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build & Test') {
            steps {
                dir("${env.SERVICE_NAME}") {
                    script {
                        echo "🏗️ Building ${env.SERVICE_NAME}..."
                        sh "mvn clean package -DskipTests=false"
                    }
                }
            }
        }

        stage('Docker Build') {
            steps {
                dir("${env.SERVICE_NAME}") {
                    script {
                        echo "🐳 Building Docker image..."
                        def imageName = "${env.PROJECT_NAME}/${env.SERVICE_NAME}:latest"
                        sh "docker build -t ${imageName} ."
                    }
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    echo "🚀 Starting Config Server..."

                    // 🔧 Crear red si no existe
                    sh '''
                        docker network inspect farmatodo-network >/dev/null 2>&1 || \
                        docker network create farmatodo-network
                    '''

                    // 🐳 Ejecutar contenedor
                    sh '''
                        docker rm -f config-server || true
                        docker run -d --name config-server \
                            -p 8888:8888 \
                            --network farmatodo-network \
                            farmatodo/config-server:latest
                    '''

                    // ⏳ Esperar a que el servicio esté arriba
                    echo "⏳ Waiting for Config Server to be healthy..."
                    sh '''
                        sleep 10
                        curl -f http://localhost:8888/actuator/health || exit 1
                    '''
                }
            }
        }
    }

    post {
        always {
            script {
                echo "=========================================="
                echo "Service: ${env.SERVICE_NAME}"
                echo "Status: ${currentBuild.result ?: 'SUCCESS'}"
                echo "Duration: ${currentBuild.durationString}"
                echo "=========================================="
            }
            junit allowEmptyResults: true, testResults: "${env.SERVICE_NAME}/target/surefire-reports/*.xml"
            archiveArtifacts artifacts: "${env.SERVICE_NAME}/target/*.jar", allowEmptyArchive: true, fingerprint: true
        }

        success {
            echo "✅ Build succeeded and service deployed!"
        }

        failure {
            echo "❌ Build or deploy failed!"
        }
    }
}
