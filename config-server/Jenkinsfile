pipeline {
    agent any

    tools {
        jdk 'java17016'
        maven 'maven399'
    }

    environment {
        // 🧩 Variables principales del servicio
        SERVICE = 'config-server'
        IMAGE_BASE = "farmatodo/${SERVICE}"
        IMAGE_TAG = "${BUILD_NUMBER}"
        IMAGE = "${IMAGE_BASE}:${IMAGE_TAG}"

        // ⚙️ Configuración de red y puertos locales
        PORT = '8888'
        HOST = '8889'
        NET = 'farmatodo-network'

        // 🔍 Herramientas y análisis
        SONAR_SCANNER_HOME = tool 'sonar7'

        // ☁️ Configuración GCP / Artifact Registry
        GCP_PROJECT_ID = 'planar-momentum-469121-n0'
        REGION = 'us-central1'
        REPOSITORY = "${SERVICE}-repo" // cada microservicio tiene su propio repo
        FULL_IMAGE_NAME = "us-docker.pkg.dev/${GCP_PROJECT_ID}/${REPOSITORY}/${SERVICE}:${IMAGE_TAG}"
    }

    options {
        timestamps()
        timeout(time: 20, unit: 'MINUTES')
    }

    stages {

        // 1️⃣ CHECKOUT Y BUILD (sin imagen aún)
        stage('Checkout & Build') {
            steps {
                echo "📦 Clonando repositorio desde GitHub..."
                checkout scmGit(
                    branches: [[name: '*/main']],
                    extensions: [],
                    userRemoteConfigs: [[
                        credentialsId: 'jenkins-gcp',
                        url: 'https://github.com/santgodev/farmatodo-backend.git'
                    ]]
                )

                dir(SERVICE) {
                    echo "🧱 Compilando y empacando ${SERVICE}..."
                    sh 'mvn clean package -DskipTests=false'
                }
            }
        }

        // 2️⃣ ANÁLISIS ESTÁTICO (SAST)
        stage('SonarQube Analysis') {
            steps {
                echo "🔍 Ejecutando análisis de código con SonarQube..."
                dir(SERVICE) {
                    withCredentials([string(credentialsId: 'sonartoken', variable: 'SONAR_TOKEN')]) {
                        withSonarQubeEnv('sonar') {
                            sh """
                                ${SONAR_SCANNER_HOME}/bin/sonar-scanner \
                                    -Dsonar.projectKey=${SERVICE} \
                                    -Dsonar.sources=. \
                                    -Dsonar.host.url=http://sonarqube-dind:9000 \
                                    -Dsonar.java.binaries=target/classes \
                                    -Dsonar.token=$SONAR_TOKEN
                            """
                        }
                    }
                }
            }
        }

        // 3️⃣ ANÁLISIS DE DEPENDENCIAS Y FS (antes del build Docker)
        stage('Trivy Source Scan') {
            steps {
                echo "🧩 Escaneando dependencias y archivos con Trivy..."
                dir(SERVICE) {
                    sh """
                        trivy fs . \
                            --format table \
                            --severity HIGH,CRITICAL \
                            -o trivy-fs-report.txt || true
                    """
                }
            }
        }

        // 4️⃣ CONSTRUCCIÓN DE IMAGEN DOCKER
        stage('Build Docker Image') {
            steps {
                dir(SERVICE) {
                    echo "🐳 Construyendo imagen Docker para ${SERVICE}..."
                    sh "docker build -t ${IMAGE} ."
                }
            }
        }

        // 5️⃣ ANÁLISIS DE IMAGEN DOCKER
        stage('Trivy Image Scan') {
            steps {
                echo "🔎 Escaneando imagen Docker con Trivy..."
                sh """
                    trivy image ${IMAGE} \
                        --format table \
                        --severity HIGH,CRITICAL \
                        -o trivy-image-report.txt || true
                """
            }
        }

        // 6️⃣ AUTENTICACIÓN Y PUSH A ARTIFACT REGISTRY
        stage('Authenticate with GCP, Tag & Push to Artifact Registry') {
            steps {
                echo 'Authenticate with GCP, tag and Push Image to Artifact Registry'
                withCredentials([file(credentialsId: 'gcp-credentials', variable: 'gcpCred')]) {
                    withEnv(["GOOGLE_APPLICATION_CREDENTIALS=$gcpCred"]) {
                        sh '''
                            echo Activating GCP service account...
                            gcloud auth activate-service-account --key-file=$GOOGLE_APPLICATION_CREDENTIALS
                            gcloud config set project $GCP_PROJECT_ID
                            echo Configuring Docker to use gcloud credentials...
                            gcloud auth configure-docker us-docker.pkg.dev --quiet
                        '''
                        script {
                            sh '''
                                gcloud artifacts repositories create ${REPOSITORY} \
                                    --repository-format=docker \
                                    --location=$REGION \
                                    --description="Docker repo for ${SERVICE}" \
                                    --project=$GCP_PROJECT_ID || true
                            '''
                            sh "docker tag ${IMAGE} ${FULL_IMAGE_NAME}"
                            sh "docker push ${FULL_IMAGE_NAME}"
                            echo "✅ Image pushed to: ${FULL_IMAGE_NAME}"
                        }
                    }
                }
            }
        }

        // 7️⃣ DEPLOY EN CLOUD RUN 🚀
        stage('Deploy to Cloud Run') {
            steps {
                echo "🚀 Desplegando ${SERVICE} en Cloud Run..."
                withCredentials([file(credentialsId: 'gcp-credentials', variable: 'gcpCred')]) {
                    withEnv(["GOOGLE_APPLICATION_CREDENTIALS=$gcpCred"]) {
                        sh '''
                            gcloud run deploy ${SERVICE} \
                                --image=${FULL_IMAGE_NAME} \
                                --platform=managed \
                                --region=${REGION} \
                                --allow-unauthenticated \
                                --project=$GCP_PROJECT_ID
                        '''
                    }
                }
                echo "✅ Despliegue en Cloud Run completado para ${SERVICE}!"
            }
        }
    }

    // 🧾 Post-Actions
    post {
        success {
            echo "✅ Build, análisis y despliegue completados con éxito para ${SERVICE}!"
        }
        failure {
            echo "❌ Falló el pipeline de ${SERVICE}!"
        }
        always {
            echo "=========================================="
            echo "Service: ${SERVICE}"
            echo "Status: ${currentBuild.result ?: 'SUCCESS'}"
            echo "Duration: ${currentBuild.durationString}"
            echo "=========================================="

            archiveArtifacts artifacts: "${SERVICE}/target/*.jar", allowEmptyArchive: true, fingerprint: true
            archiveArtifacts artifacts: "**/trivy-*.txt", allowEmptyArchive: true
        }
    }
}
